import './Mobile.scss';
function Mobile() {
    return (
        <section className="mobile">
            <p className="mobile-warning">This app is for Desktop only.</p>
        </section>
    )
}

export default Mobile
